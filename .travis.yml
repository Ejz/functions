dist: trusty
language: php
sudo: required

php:
  - 5.6

services:
  - docker

script: test -n "$TRAVIS_TAG" || { ./deploy.sh -D -P && php ./gendoc.php && ./gitci.sh "README.md" "Update README.md [skip ci]"; }
